type ext_uim_ctrl, domain;
type ext_uim_ctrl_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(ext_uim_ctrl)

# Allow ext_uim_ctrl to read vendor_toolbox exec
allow ext_uim_ctrl vendor_toolbox_exec:file r_file_perms;

# Allow ext_uim_ctrl to write to sysfs_uim_ctrl node
allow ext_uim_ctrl sysfs_uim_ctrl:file rw_file_perms;

# Allow ext_uim_ctrl to read kernel cmdline
allow ext_uim_ctrl proc_cmdline:file r_file_perms;

# Allow ext_uim_ctrl to connect to tad
socket_connect(ext_uim_ctrl, tad, tad)
